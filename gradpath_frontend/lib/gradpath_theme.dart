import 'package:flutter/material.dart';

class GPColors {
  static const bg = Color(0xFFF7F7FB);
  static const text = Color(0xFF0F172A);
  static const subtext = Color(0xFF475569);
  static const border = Color(0xFFE8E8EF);
  static const card = Colors.white;

  // Brand greens
  static const green = Color(0xFF15803D);
  static const green2 = Color(0xFF22C55E);
  static const greenSoft = Color(0xFFEAFBF0);

  // Sidebar
  static const sidebarBg = Color(0xFF0B3B2A);
  static const sidebarActive = Color(0xFF1A5C40);
  static const sidebarText = Colors.white;

  // Unique GradPath accent (adds identity)
  static const accentInk = Color(0xFF0B3B2A);

  // Risk / status
  static const riskCritical = Color(0xFFEF4444);
  static const riskModerate = Color(0xFFF97316);
  static const riskLow = Color(0xFF22C55E);
  static const riskCriticalSoft = Color(0xFFFEF2F2);
  static const riskModerateSoft = Color(0xFFFFF7ED);
  static const riskLowSoft = Color(0xFFEAFBF0);

  // Season accents
  static const fallBg = Color(0xFFFFF7ED);
  static const fallBorder = Color(0xFFFDBA74);
  static const fallAccent = Color(0xFFF97316);
  static const springBg = Color(0xFFEAFBF0);
  static const springBorder = Color(0xFF86EFAC);
  static const springAccent = Color(0xFF16A34A);
  static const summerBg = Color(0xFFFEFFE7);
  static const summerBorder = Color(0xFFFDE047);
  static const summerAccent = Color(0xFFCA8A04);
  static const winterBg = Color(0xFFEFF6FF);
  static const winterBorder = Color(0xFF93C5FD);
  static const winterAccent = Color(0xFF2563EB);

  // Info / blue
  static const blue = Color(0xFF2563EB);
  static const blueSoft = Color(0xFFEFF6FF);

  // Amber
  static const amber = Color(0xFFF59E0B);
  static const amberSoft = Color(0xFFFFFBEB);
}
