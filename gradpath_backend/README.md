# gradpath_backend

FastAPI backend for GradPath. Provides APIs for onboarding, data ingestion, and plan generation.

## Run (local)

Create a virtual environment, install dependencies, then run:

- `uvicorn app.main:app --reload`

## Environment

Copy `.env.example` to `.env` and update values.
